when LB_SELECTED {
  if { [LB::server addr] eq "10.0.0.1" } {
    # Perform custom logic for server selection
    set server_priority 10
    LB::priority $server_priority
    if {[active_members [LB::server pool]] < 2} {
      log local0. "Warning: Less than 2 active members in pool"
    }
  }
}
